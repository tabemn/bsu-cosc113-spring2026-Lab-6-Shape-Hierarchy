# -bsu-cosc113-spring2026-Lab-6-Shape-Hierarchy
Create a hierarchy of shapes using inheritance. Start with a base class Shape that has an enumerator Color (RED, GREEN, BLUE) and a method calculateArea() (which returns 0.0 in the base class). Create two concrete classes, Rectangle and Circle, that inherit from Shape.
